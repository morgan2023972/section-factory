import type { ValidationRuleInput } from "../rules/types.js";

// Phase 3 transitional alias: rules consume a single stabilized input.
export type StabilizedValidationRuleInput = ValidationRuleInput;
