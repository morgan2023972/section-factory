# Release Notes - v1.4.0

Date: 2026-03-30

## Nouveautes

- Ajout d une commande de diagnostic runtime des politiques AST:
  - `npm run dev -- --show-ast-policy`
- La commande affiche la politique AST effective chargee en JSON avec:
  - configPath
  - loadedFromEnv
  - envVar
  - ruleCount
  - policies

## Validation AST - Gouvernance runtime

- Surcharge de la politique AST possible via un fichier JSON externe sans rebuild TypeScript.
- Chemin par defaut:
  - `config/ast-rule-policies.json`
- Variable d environnement optionnelle:
  - `SECTION_FACTORY_AST_RULE_CONFIG`
- Fusion des regles:
  - base integree + overrides runtime sanitizes

## Qualite

- Ajout de tests unitaires pour verifier:
  - le chargement runtime des overrides JSON
  - le comportement du routeur avec politiques surchargees
  - la commande CLI `--show-ast-policy`
- Validation locale de la suite unitaire: OK.

## Documentation

- README aligne avec la nouvelle commande de diagnostic AST.
- ARCHITECTURE mise a jour avec la discovery `--show-ast-policy`.
- documentation-projet mise a jour avec le flux runtime AST et la sortie de diagnostic.
